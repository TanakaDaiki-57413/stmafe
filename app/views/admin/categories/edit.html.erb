<div class="container">
  <div class="row">
    <%= form_with model:@category, 
                  url:admin_category_path,
                  method: :patch do |form| %>

      <%= form.label :name ,"カテゴリ名" %>
      <%= form.text_field :name%>
      <%= form.submit "更新" %>
    <% end %>
    <%= link_to "キャンセル" ,admin_categories_path %>
  </div>
</div>